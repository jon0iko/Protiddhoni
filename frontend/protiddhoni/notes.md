Use colors from global.css, don't hard code
color theme change with something else
Remaining pages apart from lekhalikhi




## Github
git status 
use any of the 3 below:
git add -u ;adds all tracked changes
git add .  ;add all tracked or untracked changes
git add "filename"  ;

for commiting 
git commit -m "hello msg"
git push origin branch_name

create new branch
git checkout -b new_branch_name


to go to another branch:
git checkout branchname